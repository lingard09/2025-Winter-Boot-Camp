A10. 비만도 관리 프로그램 (구조체 포인터 배열 버전)

여러 명(최대 20명)의 키와 몸무게 데이터를 입력받아 모든 사람들의 비만도 목록을 만들어 출력하라.

main() 함수는 다음 코드를 사용하되, 수정할 수 없음
신장(cm단위)과 체중(kg단위)를 입력받은 후, 비만 등급을 출력하라.
단, 다음과 같은 구조체를 사용하여 프로그램을 작성하라.

struct bmi_struct{
    char name[20];  // 이름 (공백 없이 영어 알파벳으로 구성)
    int height, weight; // 키(cm), 몸무게(kg)
    float bmi; // 비만도
    int bmi_level; // 비만등급 0~4
};


단, A09 문제에서 제작한 bmiResolve() 함수를 사용해 비만등급을 판정하도록 하라.

void bmiResolve(struct bmi_struct* sp);
//파라미터: sp- 신장과 체중이 입력되어 있는 구조체 변수의 포인터
//수행내용: sp 포인터 내의 신장과 체중을 사용하여 비만도와 비만등급을 계산하여 넣는다.
//리턴값: 없음


비만도 수치 = 체중(kg) / (신장(m)의 제곱) 으로 계산
비만등급 (0~4):비만도 수치(bmi)에 따른 비만등급 판정 기준은 다음과 같다.

bmi < 18.5: 0 (Underweight),
18.5 <= bmi < 23: 1 (Normal weight),
23 <= bmi < 25: 2 (Overweight),
25 <= bmi < 30: 3 (Mild obesity),
30 <= bmi: 4 (Severe obesity)

다음 함수를 직접 제작하여 프로그램을 완성하라.

int loadData(struct bmi_struct* list[]);
// 파라미터: 채워야 하는 비만도 관리 데이터용 구조체 포인터 배열(list) 
// 리턴값: 채워진 구조체 데이터 레코드의 갯수
// 수행내용: 여러 명의 키와 몸무게의 데이터를 읽어 list 포인터 배열에 각각 메모리를 할당하여 키, 몸무게, 비만도, 비만도등급이 저장된 구조체 데이터가 연결되도록 한 후, 완성한 데이터 갯수를 리턴


void printBmiData(struct bmi_struct* list[], int size);
// 파라미터: 채워야 하는 비만도 관리 데이터용 구조체 포인터 배열(list), 데이터 개수(size) 
// 리턴값: 없음
// 수행내용: list 포인터 배열에 들어있는 각 구조체의 데이터를 출력 형식에 맞추어 화면에 출력함


다음 코드를 사용하되, 함수 제작 외에는 수정할 수 없음!

#include <stdio.h>

struct bmi_struct{
	char name[20];  // 이름 (공백 없이 영어 알파벳으로 구성)
	int height, weight; // 키(cm), 몸무게(kg)
	float bmi; // 비만도
	int bmi_level; // 비만등급 0~4
};

int loadData(struct bmi_struct* list[]);
void bmiResolve(struct bmi_struct* sp);
void printBmiData(struct bmi_struct* list[], int size);

char bmi_string[5][20] = {"Underweight","Normal weight","Overweight","Mild obesity","Severe obesity"};

int main(void) {
	struct bmi_struct* data[20];    // 최대 20명까지 관리
	int count;
	count = loadData(data);     // 여러 명의 이름, 신장, 체중 데이터 입력받기
	printf("Loaded %d\n", count);
	for(int i=0; i<count; i++)
		bmiResolve(data[i]);        // 비만도 관리 데이터 완성하기
	printBmiData(data, count);      // 비만도 관리 데이터 출력하기
	return 0;
}


입력 형식

첫 줄에 인원 수, 두번째 줄부터 인원수 만큼 이름과 2개의 정수 - 신장(cm단위)과 체중(kg단위)

출력 형식

모든 사람의 비만도 관리 데이터

예제

예제 입력 1

10
James 176 80
Mary 168 54
David 187 60
Jane 155 47
Wayne 170 75
Elice 173 80
Julie 165 50
Eugene 182 73
Amy 158 48
Steve 170 66


예제 출력 1

Loaded 10
James 176cm 80kg 25.8 Mild obesity
Mary 168cm 54kg 19.1 Normal weight
David 187cm 60kg 17.2 Underweight
Jane 155cm 47kg 19.6 Normal weight
Wayne 170cm 75kg 26.0 Mild obesity
Elice 173cm 80kg 26.7 Mild obesity
Julie 165cm 50kg 18.4 Underweight
Eugene 182cm 73kg 22.0 Normal weight
Amy 158cm 48kg 19.2 Normal weight
Steve 170cm 66kg 22.8 Normal weight
